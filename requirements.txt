# Dependencies for Aurea.
#
# TRIMMED 2026-08-15 (manifest, eighty-second entry). The prior file carried
# thirteen packages with ZERO importers anywhere in src/, scripts/, or tests/
# (numpy, scipy, sqlalchemy, psycopg2-binary, pydantic, pyyaml, matplotlib,
# seaborn, rich, asciimatics, tqdm, python-dateutil, pytest-cov) -- an import
# census across the whole tree found exactly ONE third-party import. CI
# installed all of them on every push. A dependency with no importer is not a
# capability; it is surface area. Adding one back is a decision with a named
# consumer, not a default.

# The one third-party import in the tree: src/topology/tcaml.py.
networkx>=3.1

# The suite runner. No pytest-cov: CI is deliberately minimal by design
# (CLAUDE.md section 4) -- no coverage gate exists, so its collector was
# installed for nothing.
pytest>=8.0
