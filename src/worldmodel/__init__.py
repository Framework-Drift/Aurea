"""The World Model domain - a persistent typed representation of what AUREA
thinks is happening outside herself.

**WORLD PROPOSITIONS NEVER MANUFACTURE TRUTH.** They reference kernel epistemic
records and derive standing from the kernel; a world-model inconsistency is a
conflict candidate routed into L4, where the loop M3 built adjudicates it like
anything else. The domain has no private truth machinery.
"""
